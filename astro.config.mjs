import { defineConfig } from "astro/config";

// GitHub Pages user site: https://dayvid-t.github.io (repo dayvid-t.github.io).
// If this ever moves to a project repo, add `base: "/<repo-name>"`.
export default defineConfig({
  site: "https://dayvid-t.github.io",
});
